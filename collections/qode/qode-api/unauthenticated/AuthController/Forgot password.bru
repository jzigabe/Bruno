meta {
  name: Forgot password
  type: http
  seq: 4
}

post {
  url: {{basepoint}}/auth/password/forgot?email={{$randomEmail}}
  body: none
  auth: none
}

params:query {
  email: {{$randomEmail}}
}

settings {
  encodeUrl: true
  timeout: 0
}
