info:
  name: actuator/loggers
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/actuator/mappings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
