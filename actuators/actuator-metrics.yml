info:
  name: actuator/metrics
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/actuator/loggers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
