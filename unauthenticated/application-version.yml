http:
  method: GET
  url: "{{baseUrl}}/application/name"
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/application/version"
  seq: 2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
