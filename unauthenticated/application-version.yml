info:
  name: application/version
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/application/name"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
