info:
  name: actuator/env
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/actuator/env"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
