meta {
  name: Get all questions
  type: http
  seq: 1
}

get {
  url: {{basepoint}}/questions
  body: none
  auth: inherit
}

script:post-response {
  const item = res.body[0];
  bru.setVar("questionId", item.id);
  console.log(bru.getAllVars());
}

settings {
  encodeUrl: true
  timeout: 0
}
